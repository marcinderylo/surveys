package org.adaptiveplatform.surveys.dto.generated {
import mx.events.PropertyChangeEvent;
import mx.events.ValidationResultEvent;
import mx.validators.StringValidator;
import mx.validators.ValidationResult;

/**
 * NOTE: This file is autogenerated, but will ONLY be created if it doesn't already exist.
 */
[Bindable]
[RemoteClass(alias="org.adaptiveplatform.surveys.dto.CreateStudentGroupCommand")]
public class CreateStudentGroupCommand extends CreateStudentGroupCommandBase {
    private var _valid:Boolean=true;
    public var groupNameError:String;

    public function CreateStudentGroupCommand() {
        addEventListener(PropertyChangeEvent.PROPERTY_CHANGE, validateProperties);
    }

    public function validateProperties(ignore:PropertyChangeEvent=null):void {
        _valid=true;
        var groupNameResult:ValidationResult=validateGroupName();
        if (groupNameResult) {
            groupNameError=groupNameResult.errorMessage;
            _valid=false;
        } else {
            groupNameError="";
        }
    }

    public function validateGroupName():ValidationResult {
        var groupNameValidator:StringValidator=new StringValidator();
        groupNameValidator.required=true;
        groupNameValidator.minLength=5;
        var event:ValidationResultEvent=groupNameValidator.validate(groupName, true);
        for each (var result:ValidationResult in event.results) {
            if (result.isError) {
                return result;
            }
        }
        return null;
    }

    public function get valid():Boolean {
        return _valid;
    }
}
}
